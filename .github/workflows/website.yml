name: Website

on:
  push:
    branches: [main]
    paths:
      - apps/website/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .github/actions/setup/**
      - .github/workflows/website.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# A newer push supersedes the deployment the old build would have produced.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - uses: actions/configure-pages@v6
      - run: pnpm --filter @artydog/website test
      - run: pnpm --filter @artydog/website build
      - uses: actions/upload-pages-artifact@v5
        with:
          path: apps/website/dist
      - id: deployment
        uses: actions/deploy-pages@v5
